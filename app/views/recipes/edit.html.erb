<p><%= link_to 'Back to Recipes', recipes_path(@recipe), class: 'mt-3 btn btn-light'%></p>
<div class="p-5">
<h1>Edit the timer for <%= @recipe.name %></h1>
</div>

<div class="d-flex justify-content-center" >
<%= simple_form_for @recipe do |f| %>
<%= f.input :name, input_html: { style: 'width: 400px' } %>
<%= f.input :instructions, input_html: { style: 'width: 400px' } %>
  <%= f.submit %>
  <% end %>
</div>
  <div class="p-5">
<h1>Master the Minutes: Fine-Tune Your Recipes!</h1>
  </div>
