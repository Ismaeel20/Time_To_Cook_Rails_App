<div class="d-flex justify-content-center" ><h1>Edit your Recipes!</h1> </div>

<%= simple_form_for @recipe do |f| %>
<%= f.input :name %>
<%= f.input :instructions %>
  <%= f.submit %>
  <% end %>

<p><%= link_to 'Back to Recipes', recipes_path(@recipe), class: 'mt-3 btn btn-light'%></p>
</div>
<%= link_to ('<i class="fa-solid fa-trash p-2"></i>').html_safe, recipes_path(@recipe)%>
